docker exec -it kafka bash
#Create your first topic 
/usr/bin/kafka-topics --create --topic user_events --bootstrap-server localhost:9092 --partitions 1 --replication-factor 1

#Verify the topic exists
kafka-topics.sh --list --bootstrap-server localhost:9092

#Produce a message
/usr/bin/kafka-console-producer --topic user_events --bootstrap-server localhost:9092

#second terminal
docker exec -it kafka bash
#Consume the message
/usr/bin/kafka-console-consumer --topic user_events --from-beginning --bootstrap-server localhost:9092